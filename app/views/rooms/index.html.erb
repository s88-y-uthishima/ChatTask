<h1>トップページ</h1>
<div>
  <ul>
    <% @rooms.each do |room| %>
      <li><%= link_to "ROOM#{room.id}：#{room.name}", room_path(room) %></li>
    <% end %>
  </ul>
</div>
<div>
<%= link_to "新規ROOM作成", new_room_path %>
</div>